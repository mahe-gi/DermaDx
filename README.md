# 🔬 DermaDx Lite — AI Skin Disease Classifier

A web application that uses machine learning to diagnose 6 types of erythemato-squamous skin diseases from clinical features.

> ⚠️ **For educational purposes only.** Not for actual medical diagnosis.

---

## 📋 What It Does

Predicts one of 6 skin diseases from 34 clinical features:

| # | Disease |
|---|---------|
| 1 | Psoriasis |
| 2 | Seborrheic Dermatitis |
| 3 | Lichen Planus |
| 4 | Pityriasis Rosea |
| 5 | Chronic Dermatitis |
| 6 | Pityriasis Rubra Pilaris |

**Features:**
- 🖼️ Optional image upload (auto-fills 2 features using OpenCV)
- 📋 34-feature clinical form
- 🤖 XGBoost ML model (~98% accuracy)
- 📊 Top-3 predictions with confidence scores
- 🚀 Simple web interface (Streamlit)

---

## 🚀 Quick Setup

### 1. Install Python Dependencies

```bash
cd derma-dx-lite
pip install -r requirements.txt
```

### 2. Train the Model

```bash
python train.py
```

This will:
- Load the dataset (`dermatology.csv`)
- Train the XGBoost model
- Save `model.pkl` and `feature_schema.json`

**Expected output:**
```
[Data] Loaded 366 rows from 'dermatology.csv'
[Data] Clean dataset: 358 rows, 35 columns
[Train] Training XGBoost (primary)...
[Eval] XGBoost (primary)
       Accuracy  : 0.9861
[Save] Model saved to 'model.pkl'
```

### 3. Run the App

```bash
streamlit run app.py
```

Open your browser at **http://localhost:8501**

---

## 🎮 How to Use

1. **Upload Image (Optional)** — Upload a skin image to auto-fill erythema and scaling scores
2. **Fill Features** — Adjust the 34 clinical feature sliders
3. **Click "Run Diagnosis"** — Get predictions with confidence scores
4. **View Results** — See top-3 predictions and probability chart

---

## 📁 Project Structure

```
derma-dx-lite/
├── app.py                  # Streamlit web app
├── train.py                # ML training script
├── dermatology.csv         # Dataset (366 rows, 35 columns)
├── model.pkl               # Trained model (generated by train.py)
├── feature_schema.json     # Feature definitions (generated by train.py)
├── requirements.txt        # Python dependencies
└── tests/                  # Unit tests (optional)
```

---

## 🧪 Running Tests (Optional)

```bash
pytest tests/ -v
```

---

## 📦 Dependencies

- **streamlit** — Web UI
- **xgboost** — ML model
- **scikit-learn** — Data processing
- **opencv-python-headless** — Image analysis
- **pandas, numpy** — Data manipulation
- **plotly** — Charts

See `requirements.txt` for exact versions.

---

## 🔧 Troubleshooting

**Problem:** `model.pkl not found`  
**Solution:** Run `python train.py` first

**Problem:** `ModuleNotFoundError`  
**Solution:** Run `pip install -r requirements.txt`

**Problem:** Port 8501 already in use  
**Solution:** Run `streamlit run app.py --server.port 8502`

---

## 📄 Dataset

The dataset is from the [UCI Machine Learning Repository](https://archive.ics.uci.edu/ml/datasets/dermatology):
- 366 samples
- 34 clinical + histopathological features
- 6 disease classes
- Already included in this project as `dermatology.csv`

---

## 👥 Team Setup

Each team member should:

1. **Clone/download this project**
2. **Create their own virtual environment:**
   ```bash
   python -m venv venv
   source venv/bin/activate  # On Windows: venv\Scripts\activate
   ```
3. **Install dependencies:**
   ```bash
   pip install -r requirements.txt
   ```
4. **Train the model:**
   ```bash
   python train.py
   ```
5. **Run the app:**
   ```bash
   streamlit run app.py
   ```

> **Note:** Do NOT share the `venv/` or `.venv/` folder. Each person creates their own.

---

## 📝 License

Educational project. Dataset from UCI ML Repository.
